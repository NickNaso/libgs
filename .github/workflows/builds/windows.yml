name: "Build for Windows"

on: [
  workflow_dispatch,
  pull_request
]

jobs:
  using: windows-latest
  
  run:
    name: "Build for Windows"
    runs-on: windows-latest
    
    steps:
      - name: "Checkout"
        uses: actions/checkout@v4

      - name: "Test"
        run: |
          echo "Hello, World!"