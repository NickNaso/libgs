name: "Build Windows"

runs:
  using: ${{inputs.platform}}
  steps:
    - uses: actions/checkout@v4

    - name: Build
      run: |
        echo "Hello, World!"

inputs:
  arch:
    description: 'Architecture'
    required: true
    default: 'x64'
  platform:
    description: 'Platform'
    required: true